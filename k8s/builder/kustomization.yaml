apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Administrator bootstrap, applied separately from the lotus overlay so its
# namespace transformer cannot move builder permissions into the API namespace.
resources:
  - namespace.yaml
  - serviceaccount.yaml
